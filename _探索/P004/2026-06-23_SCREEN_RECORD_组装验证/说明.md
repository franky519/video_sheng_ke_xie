# SCREEN_RECORD 组装验证

> 测试时间：2026-06-23

## 测试目的

验证 SCREEN_RECORD 执行路径 A（Playwright 真实录制 + cookie 注入 → Remotion 后期叠加）。

## 工具链

Playwright headless Chromium → webm 录制 → Remotion 读 video + CODE_UI 叠加层（Slow Push-in + 绿色扫描框）

## 结果

✅ 录制+组装完整链路跑通。场景：doubao.com 输入框逐字输入"大模型的免费额度..." → 复刻差评君 Sequence 1 的后期叠加。踩坑：WSL headless 模式、CDP cookie 注入、video 文件在 Remotion 的 staticFile 路径。
