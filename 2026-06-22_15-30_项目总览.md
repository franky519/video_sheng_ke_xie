> [!NOTE]
> **相关文档快速导航：**
> 1. [项目总览](./2026-06-22_15-30_项目总览.md)（本文件）
> 2. [目录导航](./2026-06-22_15-30_目录导航.md) — 文件入口与索引
> 3. [工程标准：物理字典与素材协议](./2026-06-22_15-30_工程标准_物理字典与素材协议.md) — 运动描述、素材标签
> 4. [端到端管线操作手册](./2026-06-22_15-30_端到端管线操作手册.md) — 步骤 0-5 操作指南
> 5. [文档依赖与影响关系图](./2026-06-22_15-30_文档依赖与影响关系图.md) — 11 条依赖链数据
> 6. [AGENTS.md](./AGENTS.md) — **行为规则权威来源**：强制阅读清单、Blast Radius 行为、任务工作流

# 项目总览

本文件只回答“这个项目要做什么、怎么生产、当前卡在哪里”。目录结构、文件入口和索引管理见：[目录导航](./2026-06-22_15-30_目录导航.md)。

创建时间：2026-06-15 17:16（北京时间）
最后更新时间：2026-06-23 13:00（北京时间）
更新次数：第 27 次（M-11：合并阶段四阶段五为"程序化渲染与迭代"，AI 执行 + 人类检查一步到位）

---

## 目录

- [配套文档](#配套文档)
- [工作区声明](#工作区声明)
- [项目愿景](#项目愿景)
- [全链路生产流程](#全链路生产流程)
  - [阶段零：从好视频里提取格式](#阶段零从好视频里提取格式)
  - [阶段一：用学到的格式写新分镜](#阶段一用学到的格式写新分镜)
  - [阶段二：素材清单提取](#阶段二素材清单提取)
  - [阶段三：素材自动获取](#阶段三素材自动获取)
  - [阶段四：程序化渲染与迭代](#阶段四程序化渲染与迭代)
  - [与端到端管线手册的职责分工](#与端到端管线手册的职责分工)
- [Few-Shot 上游：拉片与格式由来](#few-shot上游拉片与格式由来)
- [当前视频项目](#当前视频项目)
- [当前状态](#当前状态)
- [已知风险](#已知风险)

---

## 配套文档

| 文档 | 职责 |
| :--- | :--- |
| [目录导航](./2026-06-22_15-30_目录导航.md) | 管理项目目录、核心文件入口、Prompt/脚本/视频项目索引，以及文档之间的引用关系 |
| [工程标准：物理字典与素材协议](./2026-06-22_15-30_工程标准_物理字典与素材协议.md) | 规定运动描述、流媒体语法、剪辑术语、素材标签和素材资产生命周期 |
| [端到端管线操作手册](./2026-06-22_15-30_端到端管线操作手册.md) | 从文字稿到 30 秒视频的分步操作指南，定义每一步输入、输出、Agent 指令和人工检查点 |
| [省科协项目 AGENTS.md](./AGENTS.md) | 本项目专属 Agent 协作规则：强制阅读清单、Blast Radius 行为、Proposal 工作流意图 |
| [BACKLOG.md](./BACKLOG.md) | 任务看板：当前冲刺、就绪队列、待拆解、已完成，标注优先级和并行关系 |

## 工作区声明

2026-06-21 起，**省科协科普视频** 升级为独立工作主目录。Agent 进入项目后第一个读取 `./AGENTS.md`，而非上层 `general_chat/` 的全局规则。本目录下的 AGENTS.md 定义了五个文件的强制阅读清单和"任何修改前必先算 blast radius"的硬约束。与提案工作流相关的参考文件（Handoff 文档、Matt Pocock Skills 解析）已迁入本目录。

## 项目愿景

目标是：给定一篇科普文字稿，通过 AI 全链路自动化，生成一部完整的、接近差评君/冲浪Plus 质量水准的科普视频。

人类主要负责两件事：提供科普选题的文字稿，以及最终成片的质量审核与微调。中间的分镜脚本、素材清单、素材获取、程序化渲染代码和成片导出，都尽量交给 AI 按流程完成。

## 全链路生产流程

整个管线从零到成片共五个阶段。核心设计原则：每一步都用文本或结构化数据做中间产物，每一步都能独立重跑。不是为了第一次就完美——是为了每次改完上游都能从头自动再生。

```text
阶段零: 从好视频里提取格式
  好视频 → v4 Prompt 拆解 → 拆解文档（时间轴 | 原片事件 | 后期干预 | 音频 四列表格）

阶段一: 用学到的格式写新分镜
  旁白稿 + 拆解文档（Few-Shot）→ 分镜脚本（Markdown）

阶段二: 素材清单提取
  分镜脚本 → 素材清单（JSON + Markdown 概览）

阶段三: 素材自动获取
  素材清单 → assets/（PNG/JPG 素材文件）

阶段四: 程序化渲染与迭代
  分镜脚本 + assets/ → AI 写 Remotion 代码（TypeScript）→ pnpm render → 成片（MP4）
  → 人类看片、判断质量 → 不行就回上游修源头、重新跑

关键格式传导：
  拆解文档的表格结构 = 所有新增分镜脚本的格式模板。
  不是参考——是未来每个视频的分镜都必须模仿的固定格式。
```

---

### 阶段零：从好视频里提取格式

新视频不知道分镜脚本应该长什么样。不能凭空设计——得从已有好视频里学。

拿差评君的一期成品视频 → 用 v4 Prompt 指挥 AI 把它逐秒拆成 `时间轴 | 底层原片事件 | 后期干预手段 | 音频` 四列表格 → 得到一份拆解文档（当前：[AI集体涨价拉片拆解文档](./标杆视频库/差评君/C_AI集体涨价_抖音/2026-06-17_16-00_AI集体涨价_拉片拆解文档.md)）。

这份拆解文档有两层价值。第一，它记录了差评君是怎么组织画面的——什么时候切镜头、什么时候叠化、什么时候用高亮框、什么时候靠实拍素材带动情绪。第二，这个四列表格本身就是未来分镜脚本的**格式模板**。不是参考——是模板。新分镜要模仿的是这份拆解文档的结构（四个列）、粒度（Sequence 2-5 秒）、视觉语法（持久窗口壳、底部信息栏、鼠标指针、证据截图、高亮强调层），而不是 v4 Prompt 里面的规则文本。

当前 Few-Shot 状态：C 有 v4 完整拉片，D 为 v2 产物。所有新增和返工拉片统一用 v4 Prompt。

---

### 阶段一：用学到的格式写新分镜

拿到新旁白稿后，把旁白稿 + 拆解文档的 Few-Shot 样本一起喂给 AI。告诉它：**模仿拆解文档的画面节奏、视觉语法、表格结构，为新的旁白稿设计逐秒分镜。**

为什么要这样做？因为拆解文档已经把好视频的叙事规律（每段 30-40 秒一层论据、每 3-5 秒一次画面变化、抽象论点配具象视觉锚点）固化成了一套可模仿的格式。直接让 AI 对着格式填新内容，比让 AI 自己从空白设计分镜要靠谱得多。

输入：旁白稿 + Few-Shot 拆解样本 → 输出：逐秒分镜脚本（物理字典描述，不指定具体实现库）→ 存放：`视频项目/.../03_分镜脚本/`

---

### 阶段二：素材清单提取

阶段一产出的分镜脚本是给人读的设计蓝图——画面描述、设计意图、旁白节奏都有。但下一步是让 AI 去执行，AI 不能从一句"画面叠加科技感背景图"直接跳到"去 Pexels 搜 technology background"。它需要一份结构化工单：这个素材是什么类型、编号多少、能不能自动获取。

所以阶段二本质上是一个**翻译步骤**——把"画面设计语言"翻译成"资产获取指令"。分镜里每一个图层引用了一个素材，全部提取出来：编号、分类、写获取指令。

编号 `ASSET_[镜头序号]_[图层序号]` 不是随手标的——它是分镜和渲染之间的合同。第一个数字是镜头序号（对应分镜的时间段），第二个是图层序号（对应那个镜头里的第几层画面）。渲染代码用这个编号找素材文件，两边靠编号对齐。没有编号体系：分镜说"第三段中间那个图表的配图"，渲染代码不知道对应哪个 PNG 文件。

分类这一步直接决定后面能自动化到什么程度。如果分镜写的是纯代码画的元素——高亮框、弹窗、文字卡片——直接标 `[AUTO]`，不产生获取任务。其余分类（`AI_BROWSER_CAPTURE`、`STOCK_LIBRARY`、`AI_GENERATED`、`HUMAN_CAPTURE`）各自走不同的获取路径。这一步拆得清楚，下一步才能四条路径并行跑。

输入：分镜脚本 → 输出：两层——结构化 JSON（给 AI 执行）+ Markdown 概览（给人快速扫描）

> **注意：** 当前素材分类体系和阶段三的获取路径是初步设计，正在 P002 中迭代讨论。执行前查阅 `_proposals/P002_素材标签到资产协议翻译层设计.md` 的最新结论。

---

### 阶段三：素材自动获取

这是整条管线自动化程度最高的环节，也是"全自动 pipeline"目标的核心战场。理想情况下：阶段二清单里的每一条 `[TODO]` 都在这一步被 AI 自动解决——不需要人类打开浏览器截图、不需要去图库翻页亲手挑。

设计原则是**分类分流、并行执行、失败降级**：

- `GENERATE_BY_CODE`（已标 `[AUTO]`）：跳过。高亮框、弹窗、进度条、文字卡片——渲染阶段直接在 React 组件里用 CSS/SVG 画，不产生素材文件。
- `AI_BROWSER_CAPTURE`：浏览器自动化。AI 用 Playwright 打开目标网址，模拟必要交互（登录态、滚动到特定位置、等待异步加载完成），截取对应页面状态并保存为 `ASSET_xx_xx.png`。为什么不用人类手动截图？因为后续分镜可能迭代——旁白改了、镜头重排了——每次都需要可重现的截图。自动化保证一致性。
- `STOCK_LIBRARY`：图库 API。AI 按清单里的搜索关键词调用 Pexels/Unsplash/Pixabay 的 API，自动筛选匹配图片。清单里给的是一组关键词而不是一个固定链接——第一组搜不到自动切第二组、第三组，目标是尽量在这一步就搜到可用素材。
- `AI_GENERATED`：AI 图片生成。按清单给的 Prompt 调用生成 API 产出图片。
- `HUMAN_CAPTURE`：仅当 AI 确实拿不到时才标注——比如需要登录内部系统查阅的数据界面、需要签字授权才能使用的资料。目标是最小化这个类别的条目数：每多一个 `HUMAN`，管线就多一个断点。生成占位图保证流程不阻断，最终需要人类补上。

每完成一条素材，状态从 `[TODO: xxx]` 改为 `[DONE]`。渲染阶段启动时扫描清单——如果仍有 `[TODO]` 条目且对应文件不存在，脚本直接阻断告警，不拿错误占位图糊过去。

这步的终点状态：所有 AI 能自动获取的全部到位，只剩少数 `HUMAN` 条目等待人工处理。这就是自动化的边界——AI 做到的做到极致，做不到的不假装能做到。

> **注意：** 同上，阶段三的获取路径细节随 P002 迭代。执行前查阅最新结论。

---

### 阶段四：程序化渲染与迭代

分镜脚本有了、素材文件齐了——AI 把它们组装成可运行的 Remotion TypeScript 工程，输出版本就是 `pnpm render` 一条命令导出 MP4。这不是两个独立步骤——是 AI 一轮执结束的地方，人类在这里做检查和判断。

**为什么是程序化而不是手动剪辑。** 设想：阶段二发现了更好的素材来源，改了几个搜索关键词，重新跑了阶段三。手动剪辑（Premiere/剪映）意味着从头再拖一遍时间轴。程序化渲染意味着重新 `pnpm render`——代码自动把新素材放到分镜指定的时间段，改动自动体现。这就是为什么阶段一只写物理字典不写具体库。"Scale 70%→100% + Spring Damping 15" 在渲染时被直接翻译成 Remotion 的 `spring()` 调用——今天用 Remotion，明天换 GSAP，分镜脚本一个字不用改。

**渲染时 AI 怎么做。** `共享工具/库对比测试/conclusion.md` 告诉渲染 Agent 每种物理效果最适合用哪个库。CODE 类元素（高亮框、弹窗、图表）在 React 组件里直接绘制。素材文件通过 `public/assets/` 的 symlink 指到 `04_素材清单/assets/`，`asset-manifest.json` 维护编号到文件名的映射。

**看片后往下怎么走。** 输出 MP4 后人类看片评估。效果不满意，**不能直接修渲染代码**——手动调了后面没法重跑。正确做法是判断问题出在哪一环，回到那一环修源头：
- 画面节奏不对（某段太快或太慢） → 调阶段一的 Prompt 或扩充 Few-Shot 样本
- 某段素材质量差不搭调 → 回阶段三换搜索关键词或手动替换
- 动效生硬不自然 → 在阶段二分镜中细化运动描述，重新跑阶段四
- Few-Shot 拆解本身画风偏了 → 回到阶段零重拆、补更多标杆视频

因为中间产物全部是文本和结构化数据，每一步都能独立重跑。改上游 → 顺流重跑 → 看新成片 → 判断是否有效。这就是程序化管道设计的核心优势：**不是一次生成完美，而是每次都能从头迭代到满意。**

---

### 与端到端管线手册的职责分工

项目总览和管线手册各管一层：

| 项目总览（本文件） | 端到端管线操作手册 |
|---|---|
| 每个阶段**在解决什么问题** | 每个阶段**具体怎么做** |
| 为什么这一步存在、设计原则是什么 | 确切的文件路径、输入输出路径 |
| 好坏怎么判断、什么情况下要回上游 | 给 AI Agent 的口令文本 |
| 叙事层——理解管线的逻辑 | 工程执行层——实际操作时的分步指令 |

互补不重叠：项目总览不写 `pnpm render` 后面跟什么 flag 和 `cd` 到哪个目录；管线手册不重复解释"阶段零为什么要从好视频学格式"。

## Few-Shot 上游：拉片与格式由来

当前使用的拉片 Prompt 为 v4 版本：`共享工具/提示词/2026-06-17_15-50_Gemini工业级原片后期解耦拉片分析prompt_v4.txt`。v4 的核心变化是把视频拆成"底层原片事件流"和"后期干预手段"两层分析，对应拆解文档中"时间轴 / 底层原片事件 / 后期干预手段 / 音频"四列结构。具体标签定义见[工程标准：拉片素材标签](./2026-06-22_15-30_工程标准_物理字典与素材协议.md#拉片素材标签)和[场景转场术语](./2026-06-22_15-30_工程标准_物理字典与素材协议.md#场景转场术语)。

C 已有 v4 完整拉片，D 为 v2 Prompt 产物。所有新增和返工拉片统一用 v4 Prompt。视频压缩参数见 [_参考/视频压缩参考](./_参考/2026-06-22_13-38_视频压缩参考.md)。

## 当前视频项目

| 编号 | 标题 | 状态 |
|------|------|------|
| 01 | 为什么他的豆包更聪明 | 管线验证中，分镜/素材均待重新生成（见 BACKLOG T-001） |
| 02 | 内存条涨价关AI什么事 | 源文稿已完成（97 行，脚注完整），待 01 跑通后启动 |

## 当前状态

Few-Shot 样本：C 已有 v4 完整拉片，D 为 v2 完整拉片，E/A/B 待 v4 拉片。
管线验证：尚未跑通完整 30s 管线（分镜 → 素材 → 渲染 → 成片）。
详细任务分解和优先级见 [BACKLOG.md](./BACKLOG.md)。

## 已知风险

最主要的风险还是偏差逐层累积：拉片阶段模型可能误判复杂图层运动；Few-Shot 阶段模型可能分不清“优秀视觉语法”和“拉片噪音”；渲染阶段再把偏差翻译成代码，最终画面就会越来越偏。

应对方式是继续坚持三件事：拉片时强制原片/后期解耦；分镜时只写标准物理字典；渲染前用素材清单和工程标准做人工检查。
