> [!IMPORTANT]
> **修改/新增“省科协科普视频”项目下的任何文件之前，请务必先阅读并严格遵循主导航、标准与协作规则：**
> 1. [项目资源导航与工程标准.md](file:///home/fnckc/AI_workspace/coding/projects/general_chat/省科协科普视频/2026-06-09_22-03_项目资源导航与工程标准.md)
> 2. 项目根目录下的 [GEMINI.md](file:///home/fnckc/AI_workspace/coding/projects/general_chat/GEMINI.md) 与 [AGENTS.md](file:///home/fnckc/AI_workspace/coding/projects/general_chat/AGENTS.md) 协作规则
>
> 必须严格遵守**通俗化子目录边界**，以及**防偏差工程标准（闭环物理动作字典、统一素材 To-Do 协议）**。创建/更新文档时必须严格执行 `GEMINI.md` 中的**北京时间命名规范**。

# 豆包科普视频：0~30秒图层运动工程调整与待办脚本

> **最后更新时间**：2026-06-05 11:05（北京时间）  
> **更新次数**：第 4 次  
> **关联旁白源**：`2026-05-25_11-53_豆包科普视频_旁白稿_v0.1.md`  
> **对齐源草稿**：`05_写代码做视频的工程/AI_原始输出/2026-06-02_16-00_豆包科普视频_0_30s图层运动脚本.md`

本文件是针对前 30 秒视频复刻的工程落地执行文档。对原始 AI 草稿中含糊的动画表达进行了标准化物理重构，并建立了明确的资产待办清单，以供人类开发者与后续代码生成 AI 协同配合。

---

## 一、 0~30秒全局画面资产待办追踪表

> **AI 协同执行逻辑：**
> 1. **资产搜集与保存**：AI 助手（包括图像搜索、Midjourney 生成、或 OpenCV 本地绘制 subagent）自动完成特定资产（如 `ASSET_01_01`）的获取或生成后，必须将该资产文件写入本地工作区的资产文件夹：`/home/fnckc/AI_workspace/coding/projects/general_chat/省科协科普视频/01_文字稿与原始素材/assets/`，且**强行重命名为对应的资产 ID**（如 `ASSET_01_01.png`）。
> 2. **状态自动勾选 (Ticking/Checking)**：一旦图片保存就绪，执行 AI 必须自动修改下表，在“当前执行状态与本地相对路径”列中，将 `[TODO: ...]` 修改为 **`[DONE]（图片已就绪）`** 以示完成。
> 3. **未就绪告警**：渲染代码在执行合成时，若检测到表格中有标记为 `[TODO]` 的图片文件在对应目录下缺失，会主动发出告警阻断。

| 资产编号 | 资产描述与用途 | 素材分类 (Asset Class) | 生产动作与逻辑 (Action Class) | 当前执行状态与本地相对路径 |
| :--- | :--- | :--- | :--- | :--- |
| **ASSET_01_01** | 扁平化 iPhone 手机边框外壳（透明通道） | `STOCK_LIBRARY` | 公共素材下载或使用 SVG 框代替 | `[TODO: STOCK]` 待存至 01_文字稿与原始素材/assets/ASSET_01_01.png |
| **ASSET_01_02** | 豆包聊天窗口截图（普通长文本长句无排版） | `HUMAN_CAPTURE` | 人工在豆包 App 截取平庸回复 | `[TODO: HUMAN]` 待存至 01_文字稿与原始素材/assets/ASSET_01_02.png |
| **ASSET_01_03** | 豆包聊天窗口截图（精细排版、高亮对比框） | `HUMAN_CAPTURE` | 人工在豆包 App 截取高质量回复 | `[TODO: HUMAN]` 待存至 01_文字稿与原始素材/assets/ASSET_01_03.png |
| **ASSET_02_01** | 用户输入提问浮动卡片（包含提问文字） | `GENERATE_BY_CODE` | 纯 CSS 渲染：白底，16px 圆角，尺寸 900x150 | `[AUTO]` 自动代码绘制 |
| **ASSET_02_02** | 平庸回答卡片（包含平庸保险介绍） | `GENERATE_BY_CODE` | 纯 CSS 渲染：灰色底，12px 圆角，尺寸 900x500 | `[AUTO]` 自动代码绘制 |
| **ASSET_03_01** | 高质量三段式保险坑卡片（带绿/橙高亮底色） | `GENERATE_BY_CODE` | 纯 CSS 渲染：深灰色底，尺寸 1000x600 | `[AUTO]` 自动代码绘制 |
| **ASSET_03_02** | 附录小字区域局部无损放大镜容器 | `GENERATE_BY_CODE` | 纯 CSS 渲染：圆形区域，白发光边框，缩放 250% | `[AUTO]` 自动代码绘制 |
| **ASSET_04_01** | 提示词卡片 A（提问指南文本） | `GENERATE_BY_CODE` | 纯 CSS 渲染：白底黑字，尺寸 500x700，圆角 12px | `[AUTO]` 自动代码绘制 |
| **ASSET_04_02** | 提示词卡片 B（超级 Prompt 模板文本） | `GENERATE_BY_CODE` | 纯 CSS 渲染：白底黑字，尺寸 500x700，圆角 12px | `[AUTO]` 自动代码绘制 |
| **ASSET_04_03** | 3D 红色发光立体问号图标（透明通道） | `STOCK_LIBRARY` | 素材库下载 PNG 资产 | `[TODO: STOCK]` 待存至 01_文字稿与原始素材/assets/ASSET_04_03.png |

---

## 二、 分段渲染图层物理关键帧控制表

本表坐标以渲染窗口左上角为 `[0%, 0%]`，右下角为 `[100%, 100%]`。所有动画状态均翻译为标准的 CSS 属性与 React-Spring 参数，杜绝虚假 API 报错。

| 时间轴区间 | 图层解耦与素材列表 | 图层运动物理参数 (防偏差标准字典) | 文字与配音内容 | 程序化渲染实现参考 |
| :--- | :--- | :--- | :--- | :--- |
| **00:00.0 - 00:05.0** | **Layer 0 (全局背景)**：<br>• 暗冷灰色底板，RGB `[30, 32, 35]`。<br>**Layer 1 (左侧手机 A)**：<br>• 外壳使用 `ASSET_01_01`。<br>• 屏幕内容嵌入 `ASSET_01_02`。<br>**Layer 2 (右侧手机 B)**：<br>• 外壳使用 `ASSET_01_01`。<br>• 屏幕内容嵌入 `ASSET_01_03` | **Layer 0**：<br>• 静态静止，不透明度 `Opacity: 100%`。<br>**Layer 1 (手机 A)**：<br>• `[00:00.0 - 00:01.0]`：平移坐标停留在 `[TranslateX: -20%, TranslateY: 0%]`。缩放 `Scale` 从 `0%` 变至 `100%`，使用弹性缓动 `spring(damping: 15, mass: 0.6)`。<br>• `[00:02.5 - 00:05.0]`：`Opacity` 从 `100%` 线性降至 `40%`。<br>**Layer 2 (手机 B)**：<br>• `[00:01.0 - 00:02.0]`：延迟 1 秒后开始。`Scale` 从 `0%` 变至 `100%`，同样使用 `spring(damping: 15, mass: 0.6)`，停在 `[TranslateX: 20%, TranslateY: 0%]`。<br>• `[00:03.0 - 00:05.0]`：`Scale` 在 2 秒内从 `100%` 匀速缓慢增至 `105%`。 | “同样问豆包，为什么他的答案像专家，你的答案像复制粘贴？” | **双图层延迟弹性弹出与透明度分流**：<br>• **Remotion**: 容器内嵌套 `<img src="ASSET_01_01" />`，Scale 使用 React Spring 控制其弹性系数。<br>• **Moviepy**: 用 `clip.resize` 配合阻尼正弦曲线计算函数，在区间 `[0.0s, 1.0s]` 和 `[1.0s, 2.0s]` 插入插值关键帧。 |
| **00:05.0 - 00:15.0** | **Layer 0 (全局背景)**：<br>• 同上。<br>**Layer 1 (用户提问框)**：<br>• 动态绘制卡片 `ASSET_02_01`。<br>**Layer 2 (普通回答卡片)**：<br>• 动态绘制卡片 `ASSET_02_02`。 | **Layer 1 (提问框)**：<br>• `[00:05.0 - 00:05.8]`：平移垂直 Y 轴从 `TranslateY: 35%` 移动至停止点 `TranslateY: 15%`，同时 `Opacity` 从 `0%` 线性升至 `100%`。<br>• `[00:12.0 - 00:13.5]`：`Opacity` 从 `100%` 线性降至 `0%`。<br>**Layer 2 (普通回复)**：<br>• `[00:07.0 - 00:07.5]`：延迟 2 秒后。`Opacity` 从 `0%` 线性变至 `100%`，居中坐标保持在 `[TranslateX: 0%, TranslateY: -10%]`。<br>• `[00:08.5 - 00:13.5]`：屏幕文字整体垂直方向执行 `TranslateY` 匀速缓慢向上滚动 80px。<br>• `[00:13.5 - 00:15.0]`：不透明度 `Opacity` 降至 `0%`，同时模糊度 `blur` 从 `0px` 线性增至 `20px`。 | “比如你们同时问：“帮我看看这份保险条款有哪些坑。”你的豆包说，本产品保障多少种重大疾病，等待期多少天，赔付比例是多少。听完像没听。” | **卡片向上平移与高斯模糊淡出**：<br>• **Remotion**: 使用 `interpolate` 将 `frame` 分段映射。消隐阶段利用样式：`filter: blur(${blurVal}px)` 控制像素级模糊。<br>• **Moviepy**: 在合成时对 Layer 2 卡片应用自定义过滤器，利用 `cv2.GaussianBlur` 在帧写入时将核大小随着时间从 0 扩大至 21，并叠加 Alpha 透明度淡出。 |
| **00:15.0 - 00:25.0** | **Layer 0 (全局背景)**：<br>• 同上。<br>**Layer 1 (专家回复卡片)**：<br>• 动态绘制卡片 `ASSET_03_01`。<br>**Layer 2 (多色荧光高亮色条)**：<br>• 代码绘制的绿色与橙色半透明色块。<br>**Layer 3 (附录放大镜)**：<br>• 动态裁剪渲染层 `ASSET_03_02`。 | **Layer 1 (专家卡片)**：<br>• `[00:15.0 - 00:15.8]`：`Scale` 从 `90%` 弹性变至 `100%`（Spring 参数：Damping 12, Mass 0.6），居中对齐 `[TranslateX: 0%, TranslateY: 0%]`。<br>**Layer 2 (高亮色条)**：<br>• `[00:16.5 - 00:17.5]`：绿色荧光条的裁剪宽度从 `0%` 变至 `100%`（`clipPath: inset(0% 100% 0% 0%)` 变至 `inset(0% 0% 0% 0%)`）。<br>• `[00:19.0 - 00:20.0]`：橙色荧光条的裁剪宽度同理自左向右展开。<br>**Layer 3 (放大镜)**：<br>• `[00:21.5 - 00:22.5]`：定位在 `[TranslateX: 15%, TranslateY: 20%]` 处，`Scale` 从 `0%` 弹性扩展到 `100%`。<br>• `[00:22.5 - 00:25.0]`：放大镜内圈渲染的放大率固定为 `Scale: 2.5`，同时圆形中心点做幅度 2px 的正弦抖动。 | “但你朋友的豆包会直接告诉你，哪条免责条款容易漏看，哪个赔付条件看着差不多其实完全不同，甚至能提醒你去看附录里的那几行小字。” | **多通道擦除遮罩与局部缩放裁剪**：<br>• **Remotion / CSS**: 放大镜区域定义为 `<div style={{ clipPath: 'circle(120px at center)' }}>`，内部包含 Layer 1 同样的 DOM 树，但通过样式 `transform: scale(2.5)` 结合定位实现局部放大。<br>• **Moviepy / OpenCV**: 荧光笔高亮条使用 `ColorClip`，在其透明通道（Alpha）上应用带有 Easing 函数的水平线性剪裁擦除动效。 |
| **00:25.0 - 00:30.0** | **Layer 0 (全局背景)**：<br>• 同上。<br>**Layer 1 (提示词卡片 A)**：<br>• 动态绘制卡片 `ASSET_04_01`。<br>**Layer 2 (提示词卡片 B)**：<br>• 动态绘制卡片 `ASSET_04_02`。<br>**Layer 3 (红色立体问号)**：<br>• 使用图片资产 `ASSET_04_03` | **Layer 1 (卡片 A)**：<br>• `[00:25.0 - 00:25.8]`：`TranslateX` 从 `-120%` 运动至 `-12%`，自转角 `Rotate` 从 `0deg` 变化到 `-8deg`。<br>**Layer 2 (卡片 B)**：<br>• `[00:25.2 - 00:26.0]`：延迟 0.2 秒后。`TranslateX` 从 `120%` 运动至 `12%`，自转角 `Rotate` 从 `0deg` 变化到 `8deg`。<br>**Layer 3 (红色问号)**：<br>• `[00:27.0 - 00:27.5]`：定位于中心，`Scale` 从 `0%` 变至 `120%`，随后稳定至 `100%`（Spring 参数：Damping 10, Mass 0.5）。`drop-shadow` 阴影发光半径从 `0px` 增至 `30px`。 | “这时候很多人第一反应是：是不是我不会提问？是不是他的提示词写得更好？” | **双向倾斜滑入与中心发光爆裂**：<br>• **Remotion**: 通过 `useCurrentFrame` 的插值渲染来实现平移、旋转及 `drop-shadow` 的复合 CSS 动画。<br>• **Moviepy**: 用 `clip.rotate` 先制作两张倾斜卡片，再利用 `clip.set_position` 分段插值水平位置完成滑入拼接。 |

---

## 三、2026-06-03 09:53 样片执行记录

本次先用 Python + Pillow + OpenCV 做了一个 0~30 秒程序化样片，用来验证时间线和图层运动是否能从工程文档落到可播放视频。由于真实豆包截图和素材库图片尚未补齐，本次样片使用代码绘制的占位 UI，不修改上方资产表的 `[TODO]` 状态。

> 2026-06-03 10:32 更新：本节记录的是早期快速视觉草模，不再作为主执行方向。正式工程样片已改为下方“Remotion 工程执行记录”，以资产协议、物理字典、图层组件 and 关键帧导出为主线。

输出文件：

| 类型 | 路径 |
| :--- | :--- |
| 渲染脚本 | `05_写代码做视频的工程/2026-06-03_09-46_豆包科普视频_0_30s程序化样片渲染.py` |
| MP4 样片 | `05_写代码做视频的工程/2026-06-03_09-46_豆包科普视频_0_30s程序化样片输出/2026-06-03_09-46_豆包科普视频_0_30s程序化样片.mp4` |
| 关键帧预览 | `05_写代码做视频的工程/2026-06-03_09-46_豆包科普视频_0_30s程序化样片输出/关键帧预览/` |

验证结果：

| 项目 | 结果 |
| :--- | :--- |
| 分辨率 | 1280x720 |
| 帧率 | 24fps |
| 总帧数 | 720 帧 |
| 时长 | 30.0 秒 |
| 音频 | 暂无，仅验证画面运动 |

已观察到的下一版修正点：

| 问题 | 建议处理 |
| :--- | :--- |
| 部分画面信息密度偏高 | 正式版应减少单屏文字量，把旁白字幕、主标题和画面正文分开抢注意力 |
| 0~15 秒的 UI 还是“示意图感”较强 | 补齐 `ASSET_01_02`、`ASSET_01_03` 后替换为真实豆包截图，或统一改成完整模拟 App 风格 |
| 22 秒放大镜不是真正裁剪同一 DOM/画面层 | 正式 Remotion 版应使用同源图层局部放大，避免“另画一个圆形说明牌”的感觉 |
| 25~30 秒底部调试说明会被字幕压住 | 正式成片删除调试说明，只保留可见画面内容和字幕 |
| 本机未检测到 `ffmpeg` | 当前用 OpenCV 写 MP4 可用；正式长片建议安装或接入 `ffmpeg`，方便压缩、合音频和导出不同规格 |

---

## 四、2026-06-03 10:32 Remotion 工程执行记录

本次已按本文件的工程协议建立 Remotion 执行工程，不再使用 Python/OpenCV 作为主动画生成方式。当前工程用 React/Remotion 组织图层、用 CSS 绘制 `GENERATE_BY_CODE` 资产、用 `asset-manifest.json` 追踪外部资产状态，并用 `check-assets.mjs` 对 `[TODO: HUMAN]` 和 `[TODO: STOCK]` 资产做缺失检查。

| 类型 | 路径 |
| :--- | :--- |
| Remotion 工程目录 | `05_写代码做视频的工程/2026-06-03_10-20_remotion_豆包科普视频_0_30s工程样片/` |
| 工程说明 | `05_写代码做视频的工程/2026-06-03_10-20_remotion_豆包科普视频_0_30s工程样片/2026-06-03_10-32_remotion工程样片产物与修改入口说明.md` |
| Clean MP4 样片 | `05_写代码做视频的工程/2026-06-03_10-20_remotion_豆包科普视频_0_30s工程样片/out/2026-06-03_10-20_豆包科普视频_0_30s_remotion工程样片.mp4` |
| 9 秒关键帧 | `05_写代码做视频的工程/2026-06-03_10-20_remotion_豆包科普视频_0_30s工程样片/out/frame_09s.png` |
| 22 秒关键帧 | `05_写代码做视频的工程/2026-06-03_10-20_remotion_豆包科普视频_0_30s工程样片/out/frame_22s.png` |

验证结果：

| 项目 | 结果 |
| :--- | :--- |
| 分辨率 | 1280x720 |
| 帧率 | 24fps |
| 总帧数 | 720 帧 |
| 时长 | 30.0 秒 |
| TypeScript 检查 | 通过 |
| 资产检查 | 可运行，外部截图/素材仍按 `[TODO]` 报告，不伪装完成 |

修改入口：

| 修改对象 | 文件 |
| :--- | :--- |
| 四段主时间线和运动参数 | `src/composition/DoubaoScience30.tsx` |
| 秒数、帧数、字幕 | `src/lib/timeline.ts` |
| 插值、Spring、平滑曲线 | `src/lib/motion.ts` |
| 具体卡片和手机 UI 图层 | `src/components/` |
| 视觉样式 | `src/styles.css` |
| 资产状态与替换路径 | `asset-manifest.json` 与 `public/assets/` |

当前可继续修改的具体问题：

| 问题 | 下一步 |
| :--- | :--- |
| 9 秒提问卡压住了普通回答标题 | 调整 `PlainAnswerScene` 的 `questionY` 或 `.plain-answer-wrap` 顶部位置 |
| 22 秒放大镜仍像独立说明牌 | 改成同源专家卡 DOM 的局部裁切放大 |
| 外部截图和问号素材未补齐 | 按资产表补齐 `ASSET_01_01`、`ASSET_01_02`、`ASSET_01_03`、`ASSET_04_03` |
