# 豆包科普视频：0~30秒图层运动与程序化渲染脚本

> **最后更新时间**：2026-06-02 16:00（北京时间）  
> **关联旁白源**：`2026-05-25_11-53_豆包科普视频_旁白稿_v0.1.md`  
> **分辨率标准**：1920x1080 @ 30fps

本文件是针对科普视频前 30 秒的程序化视频合成设计。所有画面元素均已解耦为独立的 Layer 轨道，运动控制参数均采用物理坐标与数学插值描述，可直接翻译为 Moviepy (Python) 渲染脚本或 Remotion (React/CSS) 动效代码。

---

## 一、 0~30秒图层解耦与关键帧渲染控制表

| 时间轴区间 | 图层解耦与素材列表 | 图层运动物理参数 | 文字与配音内容 | 程序化制法与代码库参考 |
| :--- | :--- | :--- | :--- | :--- |
| **00:00.0 - 00:05.0** | **Layer 0 (底色背景)**：【纯色固态背景】暗冷灰色固态底板，RGB `[30, 32, 35]`。<br>**Layer 1 (对比手机A - 左侧)**：【复合拼贴元素】模拟手机框（PNG，800x1600），内部嵌入“快速模式”下的豆包默认聊天界面截图（包含密集、未经排版的纯文本长句回复）。<br>**Layer 2 (对比手机B - 右侧)**：【复合拼贴元素】模拟手机框（同上），内部嵌入“专家模式”下的高级豆包界面截图（带有高亮标红框、分段带符号的精细对比清单）。 | **Layer 0 (底色背景)**：<br>• `[00:00.0 - 00:05.0]`：静止播放，不透明度 100%。<br>**Layer 1 (手机A)**：<br>• `[00:00.0 - 00:01.0]`：Scale 从 0% 弹性放大至 100%（带有阻尼过冲效果），停留在左侧相对坐标 `[30%, 50%]`。<br>• `[00:02.5 - 00:05.0]`：不透明度从 100% 线性衰减至 40%（表现对比中的弱化）。<br>**Layer 2 (手机B)**：<br>• `[00:01.0 - 00:02.0]`：延迟 1s 后，Scale 从 0% 弹性放大至 100%，停留在右侧相对坐标 `[70%, 50%]`。<br>• `[00:03.0 - 00:05.0]`：Scale 在 2s 内从 100% 慢速线性缩放至 105%（强调高品质感觉）。 | 配音：“同样问豆包，为什么他的答案像专家，你的答案像复制粘贴？” | **双图层延迟弹性弹出与透明度分流**：<br>• Remotion：使用 React 物理弹簧库控制两组 `<AbsoluteFill>` 容器。设置样式：`transform: scale(${scaleB})`，其中 `scaleB = spring({ frame: frame - 30, fps, config: { damping: 15, mass: 0.6 } })`（delay 30 帧弹出）。<br>• Moviepy：自定义 `lambda t` 阻尼正弦插值函数，更新手机 A/B 的 `clip.resize` 及 `clip.set_opacity` 关键帧。 |
| **00:05.0 - 00:15.0** | **Layer 0 (底色背景)**：【纯色固态背景】同上。<br>**Layer 1 (提问框浮窗)**：【自制网页卡片】白色圆角矩形框（16px 圆角），尺寸 900x150，带有黑色加粗提问文本：“帮我看看这份保险条款有哪些坑。”<br>**Layer 2 (常规回复卡片)**：【自制网页卡片】淡灰色卡片背景（RGB `[240, 240, 240]`，12px 圆角），尺寸 900x500，带有“本产品保障 120 种重疾...”的平庸段落文本。 | **Layer 1 (提问框)**：<br>• `[00:05.0 - 00:05.8]`：相对位置从底部 `[50%, 85%]` 在 0.8s 内伴随不透明度 `0% -> 100%` 渐显，垂直向上平移至停驻点 `[50%, 65%]`。<br>• `[00:12.0 - 00:13.5]`：不透明度从 100% 降至 0%。<br>**Layer 2 (常规回复)**：<br>• `[00:07.0 - 00:07.5]`：延迟 2s 后，不透明度从 0% 变至 100%，以 `[50%, 35%]` 为中心淡入展开。<br>• `[00:08.5 - 00:13.5]`：Y 轴坐标以每秒 10 像素的速度匀速缓慢向上滚动，模拟漫不经心的阅读浏览。<br>• `[00:13.5 - 00:15.0]`：整体高斯模糊半径在 1.5s 内由 0px 线性增加至 20px，同时不透明度降至 0%（表示“听完像没听”的消隐感）。 | 配音：“比如你们同时问：“帮我看看这份保险条款有哪些坑。”你的豆包说，本产品保障多少种重大疾病，等待期多少天，赔付比例是多少。听完像没听。” | **卡片线性位移与动态高斯模糊消隐**：<br>• OpenCV / Python：对于 Layer 2 缓存图像，在时间区间 `[13.5s - 15.0s]` 时，利用当前帧与模糊内核 `cv2.GaussianBlur(img, (2*k+1, 2*k+1), 0)` 执行按帧插值渲染，不透明度做线性乘法衰减。<br>• Remotion：直接通过 React State 控制 CSS 的 `filter: blur(${blurVal}px)` 和 `opacity` 值。 |
| **00:15.0 - 00:25.0** | **Layer 0 (底色背景)**：【纯色固态背景】同上。<br>**Layer 1 (专家回复卡片)**：【自制网页卡片】高精排版圆角卡片（尺寸 1000x600），内部为深冷灰底色（RGB `[40, 42, 45]`），带有醒目的绿色加粗三段式重点说明（免责条款、赔付条件、附录小字）。<br>**Layer 2 (多色高亮选择框)**：【自制文本卡片】高度 50px 的矩形条，包括淡绿色 `rgba(74, 222, 128, 0.2)`、淡橙色 `rgba(251, 146, 60, 0.2)`。<br>**Layer 3 (附录放大镜)**：【复合拼贴元素】圆形遮罩区域（圆角半径 120px，外加发光边框），用于将 Layer 1 底部隐藏的 10px 灰暗附录文字无损放大 2.5 倍显示。 | **Layer 1 (专家卡片)**：<br>• `[00:15.0 - 00:15.8]`：Scale 从 90% 伴随弹性动效（Damping: 0.6）放大至 100%，居中在 `[50%, 50%]`。<br>**Layer 2 (多色高亮条)**：<br>• `[00:16.5 - 00:17.5]`：绿色高亮框宽度从 0px 线性延展至 500px，覆盖在第一行“免责条款”上。<br>• `[00:19.0 - 00:20.0]`：橙色高亮框宽度从 0px 线性延展至 500px，覆盖在第二行“赔付条件”上。<br>**Layer 3 (放大镜)**：<br>• `[00:21.5 - 00:22.5]`：圆形遮罩在坐标 `[65%, 70%]` 处从 Scale 0% 爆破放大至 100%。<br>• `[00:22.5 - 00:25.0]`：放大镜内圈图像微弱振动（振幅 2px，频率 5Hz），突出显微查看感。 | 配音：“但你朋友的豆包会直接告诉你，哪条免责条款容易漏看，哪个赔付条件看着差不多其实完全不同，甚至能提醒你去看附录里的那几行小字。” | **多通道擦除遮罩与局部缩放裁切**：<br>• 局部无损放大（放大镜）：在 Remotion 中，使用绝对定位的 `div` 并赋予 CSS `clip-path: circle(120px at center)`，容器内部以 `transform: scale(2.5)` 渲染局部文字，并反向调整内层坐标以保证对齐。<br>• 荧光高亮条延展：在 Moviepy 中，利用 `ColorClip` 设为半透明绿色，通过 `clip.set_duration` 配合 `resize(lambda t: (width_func(t), height))` 实现宽度插值。 |
| **00:25.0 - 00:30.0** | **Layer 0 (底色背景)**：【纯色固态背景】同上。<br>**Layer 1 (提示词卡片A)**：【自制网页卡片】卡片上印有“提问指南：作为一个保险核赔专家...”文本，圆角 12px，背景白色。<br>**Layer 2 (提示词卡片B)**：【自制网页卡片】卡片上印有“超级Prompt模板：Role: Act as...”文本，圆角 12px，背景白色。<br>**Layer 3 (红色发光问号)**：【复合拼贴元素】红色立体渲染的问号 PNG 资产（尺寸 256x256）。 | **Layer 1 (卡片A)**：<br>• `[00:25.0 - 00:25.8]`：从屏幕左侧边缘 `[-20%, 50%]` 飞入到 `[38%, 50%]`，伴随自转角度从 0 变为 -8 度。<br>**Layer 2 (卡片B)**：<br>• `[00:25.2 - 00:26.0]`：延迟 0.2s 后从右侧边缘 `[120%, 50%]` 飞入到 `[62%, 50%]`，伴随自转角度从 0 变为 8 度。<br>**Layer 3 (红色问号)**：<br>• `[00:27.0 - 00:27.5]`：在两张卡片交界处的屏幕中心 `[50%, 50%]` 瞬间强力弹出，Scale 0% -> 120% -> 100%（具有极高阻尼的弹性缩放），并伴随发光半径从 0px 突变至 30px（闪烁动效）。 | 配音：“这时候很多人第一反应是：是不是我不会提问？是不是他的提示词写得更好？” | **双向偏转飞入与坐标矩阵旋转**：<br>• 旋转与飞入：在 Moviepy 中通过 `clip.rotate(-8)` 以及位置插值完成；在 Remotion 中使用 `transform: translate3d(x, y, 0) rotate(-8deg)`。<br>• 问号爆裂与发光：在 Remotion 中通过 CSS 的 `filter: drop-shadow(0 0 20px rgba(255,0,0,0.8))` 以及 React-Spring 缩放差值完成。 |
