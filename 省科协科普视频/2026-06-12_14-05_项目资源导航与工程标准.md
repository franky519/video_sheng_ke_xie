> [!IMPORTANT]
> **修改/新增"省科协科普视频"项目下的任何文件之前，请务必先阅读并严格遵循主导航、标准与协作规则：**
> 1. [项目资源导航与工程标准.md](file:///home/fnckc/AI_workspace/coding/projects/general_chat/省科协科普视频/2026-06-12_14-05_项目资源导航与工程标准.md)
> 2. 项目根目录下的 [GEMINI.md](file:///home/fnckc/AI_workspace/coding/projects/general_chat/GEMINI.md) 与 [AGENTS.md](file:///home/fnckc/AI_workspace/coding/projects/general_chat/AGENTS.md) 协作规则
>
> 必须严格遵守**通俗化子目录边界**，以及**防偏差工程标准（闭环物理动作字典、统一素材 To-Do 协议）**。创建/更新文档时必须严格执行 `GEMINI.md` 中的**北京时间命名规范**。

# 项目资源导航与工程标准

本文件是"省科协科普视频"程序化复刻工程的全局主入口，用于规范代码库映射、素材资产待办流程以及管理各子目录内的核心文件。

最后更新时间：2026-06-12 14:05（北京时间）
更新次数：第 11 次

---

## 项目愿景与全链路生产流程

### 终极目标

给定一篇科普文字稿，通过 AI 全链路自动化，生成一部完整的、接近差评君/冲浪Plus 质量水准的科普视频。

### 人类与 AI 的分工边界

**人类只负责两件事**：
1. 提供科普选题的文字稿（内容方向、核心论点、大概讲什么）
2. 最终成片的质量审核与微调

**其余所有环节由 AI 自动完成**。

### 全链路流程

```
文字稿（人类提供）
    │
    ▼
┌─────────────────────────────────────────────┐
│ 第一步：分镜头脚本生成                        │
│                                             │
│ 输入：文字稿 + Few-Shot 拆解样本             │
│ 输出：逐秒分镜脚本（物理字典描述，不含库）    │
│ 存放：07_分镜与素材/分镜脚本/                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 第二步：素材清单提取                          │
│                                             │
│ 输入：分镜头脚本                              │
│ 输出：结构化素材待办清单（编号/分类/获取指令） │
│ 存放：07_分镜与素材/素材清单/                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 第三步：素材自动获取                          │
│                                             │
│ · 界面截图 → 浏览器自动化访问并截取            │
│ · 通用素材 → 图片候选池检索                    │
│   （每个 TODO 自动产出 6-10 张候选卡片；        │
│    Google AI Mode 暂不作为生产 API）           │
│ · 代码素材 → 渲染代码直接生成                  │
│                                             │
│ 候选池：07_分镜与素材/素材候选池/             │
│ 最终素材：07_分镜与素材/assets/              │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│ 第四步：程序化渲染合成                        │
│                                             │
│ 输入：分镜脚本 + 素材                         │
│     + 库对比测试结论（物理描述→最佳库映射）    │
│                                             │
│ 输出：完整视频（Remotion 渲染）               │
└─────────────────────────────────────────────┘
```

数据流总览：

```
01_文字稿  ──┐
             ├→  AI  →  07_分镜脚本/  →  AI  →  07_素材清单/  →  AI+人  →  07_assets/
02_拆解样本 ─┘                                                              │
04_Prompt ───┘                                                              │
                                                                            ▼
                                                              05_渲染工程（消费 07_assets/）
                                                              06_库映射（辅助 05_ 选库）
```

#### 第一步：分镜头脚本生成（详细说明）

分镜头脚本是整条链路的核心产物。**不是**粗略的事件流（"这里有个卡片弹出来"），而是一份用物理字典精确描述画面的完整生产文档。

**注意：分镜脚本只使用物理字典描述动画（Scale/Opacity/Blur/Spring 等），不指定具体实现库。** 库的选择属于第四步渲染阶段的决策。

**输入**：

| 输入 | 来源 | 说明 |
| :--- | :--- | :--- |
| 文字稿 | 人类提供（`01_文字稿与原始素材/`） | 科普选题的文案内容 |
| 标杆视频 Few-Shot 拆解样本 | `02_别人视频的拆解分析/` | 差评君/冲浪Plus 等标杆视频经 Gemini 多模态拉片后的结构化脚本，AI 从中学习视觉语法 |

**拆解工具链**（用于生成 Few-Shot 样本）：
- 视频分析脚本（OpenRouter）：`03_视频抽帧与裁剪工具/2026-06-02_09-45_openrouter_analyze_30s.py`
- 视频分析脚本（直连 Files API）：`03_视频抽帧与裁剪工具/2026-06-02_17-45_gemini_native_analyze_30s.py`
- 全长分段拉片脚本：`03_视频抽帧与裁剪工具/2026-06-10_21-00_openrouter_segment_analyze.py`
- Gemini 接入教程：`03_视频抽帧与裁剪工具/2026-06-10_20-30_Google_Vertex_AI_Gemini接入教程.md`
- **当前使用的分析 Prompt（v2）**：`04_教AI拉片的提示词/2026-06-09_17-30_Gemini物理拉片分析prompt_v2.txt`
  - v2 相比 v1 的改动：删除"程序化制法与代码库参考"列；新增设计意图（用 → 连接运动与叙事目的）；素材打标分类（`[BROWSER]`/`[CODE]`/`[STOCK]`/`[AI_GEN]`/`[HUMAN]`）；静态层单独声明不在时间轴重复
- 旧版 Prompt（v1/v0，已归档）：`04_教AI拉片的提示词/_归档_旧版prompt/`
- 视觉语法迁移示例：`04_教AI拉片的提示词/2026-06-04_11-40_豆包前30秒Few-Shot视觉语法迁移示例.md`

**拆解生产方案**（详见 `02_别人视频的拆解分析/_工程计划/2026-06-10_19-00_视频拉片分段策略实验计划.md`）：
- 渠道：OpenRouter + gemini-3.5-flash (reasoning=high)
- 分段策略：30s/段，5s 重叠缓冲，每个视频约 12 段
- 合并：OpenCode + DeepSeek V4 Pro 非交互模式合并分段结果
- 仅接受 gemini-3.5-flash 的拆解结果，gemini-2.5-flash 质量不达标已归档

**分镜脚本需要精确到**：
- 每个图层的内容、构图意图与视觉层级（观众先看哪里）
- 物理动画参数（如"Scale 0→100%, spring damping 15, mass 0.6"）
- 文字/配音与画面的时间对齐
- 情绪意图（为什么用这种节奏、这段是制造紧张感还是给观众喘息）

**产出位置**：`07_分镜与素材/分镜脚本/`

#### 第二步：素材清单提取（详细说明）

分镜脚本生成后，单独进行一轮解析，从中提取每个图层引用的素材，逐个归类、编号、写获取指令。这是对分镜的**后处理**，不是同步输出——让每一步的任务更聚焦，减少 AI 出错。

**产出位置**：`07_分镜与素材/素材清单/`

#### 第三步：素材自动获取（详细说明）

每个 `[TODO]` 素材经过以下生命周期：

```
[TODO] → AI 改写为 3 组搜索词（中文语义/英文素材/视觉风格）
      → 并行调用 Pexels / Unsplash / Pixabay / Openverse（必要时补 Brave Image Search）
      → AI 去重、过滤低清/水印/不合画幅
      → [CANDIDATES_READY] 生成 6-10 张候选卡片（存入素材候选池/ASSET_xx_xx/）
      → 人选中 1 张
      → [SELECTED] 下载到 assets/ASSET_xx_xx.png
      → [DONE] 更新素材台账 asset_ledger.json
```

**候选池位置**：`07_分镜与素材/素材候选池/ASSET_xx_xx/candidates.json`
**最终素材位置**：`07_分镜与素材/assets/ASSET_xx_xx.png`

#### 第四步：程序化渲染合成（详细说明）

渲染阶段才引入库选型决策。输入除了分镜脚本和素材外，还需要 `06_库对比测试/conclusion.md` 中的"特效→最佳库"映射表（格式：全局汇总表 + 按库聚合统计 + 各 case Gemini 评判原文，case 1 已完成评判），确保每种特效使用实测验证过的最优库，而非 AI 凭训练数据猜测。

### 当前进度与核心缺口

**缺口一：Few-Shot 拆解样本严重不足**

| 标杆视频 | 位置 | 已有高精度拆解 | 质量判断 | 缺口 |
| :--- | :--- | :--- | :--- | :--- |
| 差评君·AI集体涨价（抖音） | `02_/差评君/C_AI集体涨价_抖音/` | 全长分段拉片（gemini-3.5-flash, v2 prompt） | **格式达标，质量最高** | 基本完成，可能需合并优化 |
| 差评君·信用卡APP（抖音） | `02_/差评君/D_信用卡APP_抖音/` | 全长分段拉片（gemini-3.5-flash, v2 prompt） | **格式达标** | 基本完成 |
| 差评君·豆包是真人炼的（抖音） | `02_/差评君/E_豆包是真人炼的_抖音/` | 无 | 未拉片 | 需按标准流程拆解 |
| 差评君·在太空搞数据中心（B站） | `02_/差评君/A_在太空搞数据中心_B站/` | 前5分钟抽帧（旧方法） | 旧版，无设计意图 | 需用 v2 prompt + 3.5-flash 重新拆解 |
| 冲浪普拉斯·迅雷荒诞二十年（B站） | `02_/冲浪普拉斯/B_迅雷荒诞二十年_B站/` | 前5分钟抽帧（旧方法） | 旧版，不可用 | 需从头按标准拆解 |

每个标杆视频需要完整拆解，才能覆盖完整的视觉语法（开场钩子 + 论证段 + 段落过渡 + 情绪曲线转折）。30 秒只有开头，AI 生成的分镜在 30 秒后必然走样。

**缺口二已部分解决：v2 Prompt 补齐了设计意图和素材分类**

v2 Prompt 改进后，拆解输出已从"事件流"升级为包含设计意图的分镜：
- 每个运动描述附带 `→` 叙事目的（注意力引导、情绪效果、信息节奏）
- 素材带分类标签（`[BROWSER]`/`[CODE]`/`[STOCK]` 等），不同类型用不同描述精度
- 静态层抽离为"持久画面架构"，不在时间轴重复

仍待解决：库选型（属于第四步）、多段合并后的一致性。

**下一步优先事项**：
1. **端到端管线跑通**：按 `2026-06-12_14-05_端到端管线操作手册.md` 的 5 步流程，用现有 C/D 两份高质量拉片样本跑通一次 30 秒视频，验证管线可行性
2. 视频 E（豆包是真人炼的）按标准流程拆解
3. 视频 A/B 用 v2 prompt + 3.5-flash 重新拆解
4. 库对比测试（`06_库对比测试/`）完成剩余 case 的评判（10 个 case 中已完成 1 个）

**已知风险：偏差逐层累积**

即使上述缺口全部补齐，仍需警惕三层偏差的传导：拆解阶段 Gemini 对复杂图层运动的理解可能有偏差（如误判多图层复合运动）→ Few-Shot 阶段 AI 可能无法区分"优秀细节"与"拉片噪音"，导致逻辑放大 → 渲染阶段偏差累积使最终画面失控。应对方式见素材协议中的"草稿隔离"规则。

---

## 项目子目录资源地图

当前项目编号体系及其在生产链路中的角色：

| 编号 | 名称 | 角色 | 生产链路位置 |
|------|------|------|-------------|
| `01_` | 文字稿与原始素材 | 人类输入 | 起点 |
| `02_` | 别人视频的拆解分析 | 参考学习 | Few-Shot 样本 |
| `03_` | 视频抽帧与裁剪工具 | 工具 | 拆解执行 |
| `04_` | 教AI拉片的提示词 | Prompt | 控制拆解质量 |
| `05_` | 写代码做视频的工程 | 渲染 | 最终合成 |
| `06_` | 库对比测试 | 测试 | 辅助渲染决策 |
| `07_` | 分镜与素材 | 核心产物 | 分镜→清单→素材 |

### 01_文字稿与原始素材

人类提供的文案输入层。

- 旁白脚本：`2026-06-12_14-00_豆包科普视频_旁白稿_v0.2.md`
- 源文稿：`源文稿/为什么他的豆包更聪明.md`
- 背景资料：`既有科普视频资料/`（大赛通知、获奖名单、大纲等）
- 旧版素材表（已归档）：`_归档_旧版素材表/`（旧视觉方案的 shots.csv 和素材实现表）

### 02_别人视频的拆解分析

标杆视频的拉片分析结果。按 UP 主归类，共 5 个参考视频来自 2 个 UP 主。

```
02_别人视频的拆解分析/
├── 差评君/
│   ├── A_在太空搞数据中心_B站/     前5分钟抽帧+contact sheet（旧方法）
│   ├── C_AI集体涨价_抖音/          全长分段拉片（当前质量最高的 Few-Shot 样本）
│   │   ├── source_meta.md
│   │   ├── raw_video/
│   │   ├── 分段拉片结果/ (S01-S09)
│   │   └── 完整拉片分析_merged.md
│   ├── D_信用卡APP_抖音/           全长分段拉片
│   └── E_豆包是真人炼的_抖音/      待拉片（raw_video 已就绪）
│
├── 冲浪普拉斯/
│   └── B_迅雷荒诞二十年_B站/       前5分钟抽帧+contact sheet（旧方法）
│
├── _参考资料/                       节奏统计、下载记录、内容农场参考
├── _工程计划/                       拉片分段策略、任务确认等工程文档
└── _归档_旧版结果/                  v1 prompt / gemini-2.5-flash 等过时结果
```

仅 gemini-3.5-flash (v2 prompt) 的结果作为有效 Few-Shot 样本。旧版结果已移入 `_归档_旧版结果/`。

### 03_视频抽帧与裁剪工具

视频裁剪、抽帧、多模态 API 通信的 Python 脚本和接入教程。

核心脚本：
- `2026-06-10_21-00_openrouter_segment_analyze.py`（全长分段拉片，当前主力）
- `2026-06-02_17-45_gemini_native_analyze_30s.py`（Gemini Files API 原生视频分析）
- `2026-06-02_09-45_openrouter_analyze_30s.py`（OpenRouter 单段分析）

辅助文档：
- `2026-06-10_20-30_Google_Vertex_AI_Gemini接入教程.md`

### 04_教AI拉片的提示词

视频拉片的 Prompt 模板与标准化文档。

当前在用：
- **`2026-06-09_17-30_Gemini物理拉片分析prompt_v2.txt`**（核心，v2）
- `2026-06-04_11-40_豆包前30秒Few-Shot视觉语法迁移示例.md`
- `2026-06-05_Gemini视觉设计系统提取prompt.txt`
- `2026-06-02_11-45_视频分析Prompt重构与拉片标准.md`（方法论文档）

已归档：`_归档_旧版prompt/`（v0、v1 已废弃）

### 05_写代码做视频的工程

程序化渲染 Remotion 工程。

当前在用：
- `2026-06-03_10-20_remotion_豆包科普视频_0_30s工程样片/`（Remotion 工程目录）

已归档：`_归档_旧版方案/`（旧视觉方案的 motion 视觉板、30s prototype、程序化样片、旧版图层运动脚本等）

### 06_库对比测试

每种特效让不同动效库各自实现同一画面需求，Gemini 评判最佳库。详细方案见 `handoff.md`。Remotion 为框架层不参与比对；视觉层比对 GSAP / Motion / Anime.js / PixiJS / CSS 原生 / Canvas 2D。产出 `conclusion.md`（特效→最佳库映射表，已有框架且 case 1 已完成评判，共 10 个 case）。

### 07_分镜与素材

生产链路的核心中间产物层。从分镜脚本到最终素材的全流程。

```
07_分镜与素材/
├── 分镜脚本/          AI 基于文字稿 + Few-Shot 样本生成的逐秒分镜
├── 素材清单/          从分镜脚本提取的结构化素材待办清单
├── 素材候选池/        自动找图的中间产物（每个素材 6-10 张候选卡片）
│   └── ASSET_xx_xx/
│       ├── candidates.json
│       └── thumbnails/
└── assets/            最终选定的素材文件 + 素材台账
    ├── ASSET_xx_xx.png
    └── asset_ledger.json
```

素材生命周期详见下方"统一素材 To-Do 协议"。

---

## 防偏差工程标准：闭环物理动作字典

为了防止大模型在分析视频或输出动效时凭空捏造（Hallucinate）不存在的、或者不兼容的第三方库（如 Moviepy/Remotion）私有 API 导致其他 AI 编写代码时报错，我们规定所有"运动与渲染"的描述必须翻译为**"标准网页底层物理字典"**。

在提示词与脚本中，只允许大模型映射至以下通用属性：

### 1. 空间定位（基准百分比）
* 所有的位置坐标一律以左上角为基准点 `[0%, 0%]`，右下角为 `[100%, 100%]`。
* 严禁直接使用设备相关的固定像素值（如 `1920x1080` 下的特定像素点），必须写成相对百分比坐标，例如 `位置：[50%, 50%]`（居中）。

### 2. 标准物理变换字典（CSS/JS Primitives）
动效拉片中对"运动"的程序化描述，必须直接映射到如下标准物理量：
* `Scale (缩放)`：以基准值 `100%` 进行变化。
* `Opacity (不透明度)`：取值范围 `0%`（完全透明）至 `100%`（完全可见）。
* `TranslateX / TranslateY (相对平移)`：用相对窗口宽高的百分比表示（例如：`TranslateX 从 100% 移至 0%` 表示自右滑入）。
* `Rotate (角度旋转)`：用顺时针角度度数表示（例如 `rotate(-15deg)`）。
* `Blur (高斯模糊)`：用模糊像素半径表示（例如 `blur(15px)`）。
* `ClipPath (裁剪遮罩)`：采用 CSS 的矩形滑块语法（例如 `clipPath: inset(0% 100% 0% 0%)` 表示水平方向向右擦除露出）。

### 3. 标准插值与缓动控制（Easing & Springs）
* **线性插值（Linear）**：即在指定秒数内匀速变化。
* **弹性物理函数（Spring）**：必须提供两个关键控制量：
  * `Damping (阻尼系数)`：一般介于 `10 - 20` 之间，越低越弹，通常 `12` 为经典回弹，小于 `10` 容易产生反复震荡。
  * `Mass (质量系数)`：一般为 `0.5 - 1.0`，越低越轻，弹出速度越快。
* **贝塞尔曲线控制（Bezier）**：必须指定起止控制点参数（例如 `cubic-bezier(0.25, 0.1, 0.25, 1.0)`）。

---

## 资产追踪工程标准：统一素材 To-Do 协议

为了把抽象的"镜头描述"彻底转为工程上可被 AI 检查和人类核对的待办清单，脚本中引用的所有非代码生成资产，都必须遵循以下**资产 To-Do 协议**。

### 1. 资产分类与命名规范
每个素材编号为 `ASSET_[镜头序号]_[图层序号]`（如 `ASSET_01_02.png`），统一存入 `07_分镜与素材/assets/`。

| 素材分类 | 生产方式 | 状态标记 |
| :--- | :--- | :--- |
| **`GENERATE_BY_CODE`** | 纯代码绘制（CSS/SVG/渐变背景），无外部依赖 | `[AUTO]` |
| **`AI_BROWSER_CAPTURE`** | AI 通过浏览器自动化访问实际网页并截取（如 ChatGPT 界面、新闻页面） | `[TODO: BROWSER]` |
| **`STOCK_LIBRARY`** | 搜索引擎/图库检索的公共素材（实拍 B-roll、图表等） | `[TODO: STOCK]` |
| **`AI_GENERATED`** | 提示词驱动 AI 生成的图片（如 Midjourney），需记录 Prompt | `[TODO: AI_GEN]` |
| **`HUMAN_CAPTURE`** | 确实只有人类才能获取的素材（极少数情况） | `[TODO: HUMAN]` |

### 2. AI 自动化执行规则

1. **自动归网与命名**：AI 获取或生成资产后，必须重命名为对应资产 ID 并存入 `07_分镜与素材/assets/` 目录。
2. **状态自动勾选**：图片就绪后，将对应文档中的 `[TODO: ...]` 修改为 `[DONE]`。
3. **未就绪告警**：渲染时如发现 `[TODO]` 状态的资产文件不存在，脚本必须阻断并告警。
4. **草稿隔离**：AI 原始输出必须原样保存在对应的归档目录中，人工校正在独立文件中进行，防止源头污染。

### 3. 素材候选池工作流

每个 `[TODO: STOCK]` 素材的候选池存放于 `07_分镜与素材/素材候选池/ASSET_xx_xx/`：

| 字段 | 作用 |
| :--- | :--- |
| `asset_id` | 对应 `ASSET_xx_xx`，防止图片和镜头错位。 |
| `thumbnail_url` | 用来快速预览 6-10 张候选。 |
| `download_url` | 选中后用于下载原图或合适尺寸图。 |
| `source_page_url` | 以后追溯来源和授权。 |
| `provider` | Pexels / Unsplash / Pixabay / Openverse / Brave 等。 |
| `author` | 署名需要。 |
| `license` | 授权检查需要。 |
| `fit_reason` | AI 说明为什么它适合这个镜头。 |
| `risk_note` | 比如"可能有商标""授权需复核""画幅太窄"。 |

### 4. 素材台账

最终选定的素材在 `07_分镜与素材/assets/asset_ledger.json` 中记录完整的来源和授权信息，字段包括：`asset_id`、`source_url`、`provider`、`license`、`author`、`fetched_at`、`status`。

### 5. 推荐图库工具

| 工具 | 优先级 | 适合用途 |
| :--- | :--- | :--- |
| Pexels API | 第一优先 | 实拍照片、人物、办公、科技感 B-roll |
| Unsplash API | 第二优先 | 高质量摄影图、氛围图 |
| Pixabay API | 补充 | 插画、图标、透明感素材 |
| Openverse API / Wikimedia Commons | 补充 | 公共领域、CC 授权、历史/科学图 |
| Brave Image Search API | 补充 | 全网灵感，授权需人工复核 |

不推荐：Google AI Mode（无公开 API）、Bing Image Search API（已退役）、Google Programmable Search（已停止新客户）。

---

## 增量问答 / QA

### 2026-06-12 12:46：新增端到端管线操作手册

新增 `2026-06-12_14-05_端到端管线操作手册.md`，定义了从文字稿到 30 秒视频的 5 步操作流程（分镜脚本生成 → 素材清单提取 → 素材获取 → Remotion 代码生成 → 渲染）。每步明确了输入/输出/AI Agent 指令/人工检查点。核心目的是建立快速反馈回路：改上游数据 → 跑管线 → 看视频 → 判断改动是否有效。同步将"端到端管线跑通"提升为下一步优先事项第 1 条。

### 2026-06-11 20:00：项目文件结构重组

执行了全项目文件结构重组，主要变更：

1. **02_ 按 UP 主归类**：原来按平台/话题散开的视频（抖音AI涨价、抖音信用卡APP）统一归入 `差评君/` 目录，标注来源平台。5 个参考视频全部来自差评君（4个）和冲浪普拉斯（1个）。
2. **新增 07_分镜与素材**：独立出分镜脚本、素材清单、素材候选池、最终素材的完整生命周期目录，不再混在 01_ 或 02_ 中。
3. **各目录旧版产物归档**：01_ 旧版素材表、04_ 旧版 prompt、05_ 旧版方案（手机壳视觉方案）统一归入各自的 `_归档_旧版xxx/` 子目录。
4. **02_ 散落文件归位**：工程计划文档归入 `_工程计划/`，参考资料归入 `_参考资料/`，旧的 v2 单次分析结果归入 `_归档_旧版结果/`。
5. **消除冗余**：删除了 `2026-06-02_抖音参考视频切片分析包/`（下载中转站，raw 已分发）和 `2026-05-31_10-47_差评君冲浪普拉斯视频切片分析包/`（内容已按 UP 主重组），以及根目录的 `temp_frames/` 和 `__pycache__/`。

### 2026-06-09 22:03：怎样复刻"Google AI Mode 一次给 6-10 张图让我挑"的体验？

已整合到上方"素材候选池工作流"和"推荐图库工具"章节。

### 2026-06-09 20:38：Google AI Mode 能否作为素材自动找图 API？

结论：不能。Google AI Mode 是前台搜索体验，没有公开 API。工程方案应使用 Pexels/Unsplash 等图库 API + Gemini 改写搜索词。详见上方素材协议。
