# 文档依赖与影响关系图

> 创建时间：2026-06-20 22:55（北京时间）
> 最后更新：2026-06-25 18:00（北京时间）
> 更新次数：9（M-10：链 10 修正 —— 旧 Codex skill 方案归档，Proposal 标记为 Superpowers 驱动）

## 这个文件有什么用

本文件是"省科协科普视频"项目的**结构性依赖地图**。它把项目里所有关键文档之间的依赖关系显式写出来——谁引用谁、谁的定义被谁消费、改一个文件会波及哪些文件。

三个核心入口文档（项目总览、目录导航、工程标准）的 IMPORTANT 区块里都引用了本文件。任何一个 AI session 在修改项目文件之前，读完核心入口文档后，接着读本文件，就能自动算出完整的 blast radius

读本文件时，结构性修改请直接对照 11 条链条逐文件做手动 blast radius 分析即可。

---

## 全局路径约定

本文件中出现的所有文件名和脚本名（如 `openrouter_segment_analyze.py`、`旁白稿.md`、`完整分镜头脚本.md`）均为**简化标识**，不携带时间戳前缀。实际精确文件名和当前入口路径请先查询[目录导航](./2026-06-22_15-30_目录导航.md)。做 `rg` 或路径匹配时，用简化标识匹配即可，AI 应先从目录导航获取当前精确路径再操作文件。

举例：链 1 写 `openrouter_segment_analyze.py` 时，真实文件名为 `2026-06-10_21-00_openrouter_segment_analyze.py`。链里用简写，执行时从目录导航查到带时间戳的完整名。

---

## 文档层级总览

```text
                     ┌──────────────────────────┐
                     │     项目总览               │
                     │  "What & Why"             │
                     │  愿景、管道、进度、缺口      │
                     └──────────┬───────────────┘
                                │ 引用
         ┌───────────────────────┼───────────────────────┐
         ▼                       ▼
 ┌───────────────┐   ┌─────────────────────┐
 │  目录导航      │   │  工程标准：           │
 │  "Where"      │   │  物理字典与素材协议    │
 │  目录结构、入口 │   │  "How"              │
 └───────┬───────┘   │  运动描述、标签、协议   │
         │           └──────────┬──────────┘
         │                      │
         └──────────────────────┼───────────────────────┘
                               │ 共同消费
                    ┌──────────┴──────────┐
                    │   视频项目产物        │
                    │  分镜脚本 → 素材清单   │
                    │  → 渲染工程 → 成片    │
                    └─────────────────────┘

                    ┌────────────────────────────────┐
                    │   Proposal 工作流 [活跃]     │
                    │   Superpowers → _proposals/  │
                    └────────────────────────────────┘
```

---

## 一、核心入口文档的三角关系

这三个文档是项目的地基。每个文档的 IMPORTANT 区块都互相引用，形成闭环。

```
项目总览 ←→ 目录导航
   ↕           ↕
      工程标准
```

**职责边界（不交叉）：**

| 文档 | 管什么 | 不管什么 |
|------|--------|---------|
| 项目总览 | 愿景、生产流程、当前进度、缺口、下一步 | 不管理目录结构和文件路径索引 |
| 目录导航 | 目录结构、文件入口、Prompt/脚本/视频产物索引、引用维护规则 | 不描述管线步骤和 Agent 指令 |
| 工程标准 | 运动描述约束、素材标签定义、素材资产生命周期、推荐工具 | 不描述"先做哪步后做哪步" |

**关键约束：** 同一个信息只能存在于三个文档中的**一个**里。如果发现工程标准里定义了素材标签、目录导航里又定义了一遍——必须在一次 proposal 里合并到一个文档，另一个文档改为引用。

---

## 二、十一条核心依赖链

每条链标注了"改动起点 → 自动影响的文件 → 为什么"。

---

### 链 1：Prompt 版本升级（v3 → v4，或 v4 → v5）

这是最复杂的 blast radius，因为 Prompt 是整条拉片管线的上游定义者。

**起点：** 新增或修改 `共享工具/提示词/Gemini工业级原片后期解耦拉片分析prompt_vN.txt`

**关键格式传导：** v4 Prompt 生成的拆解文档（`时间轴 | 底层原片事件 | 后期干预手段 | 音频` 四列表格）直接决定了分镜脚本应该长什么样。改 Prompt → 拆解文档格式可能变 → 分镜脚本的格式模板跟着变。不要把 v4 Prompt 当上游终点的定义——拆解文档的表格结构才是新分镜要模仿的格式源头。

| 受影响文件 | 原因 | 具体要改什么 |
|-----------|------|-------------|
| **工程标准** | v4 Prompt 里强制了素材打标（5 类）、转场类型（4 种）、原片/后期解耦等要求——工程标准里"拉片素材标签"和"场景转场术语"两章是这些要求的**镜像定义**。Prompt 改了标签类型或术语定义，工程标准必须同步 | 素材标签章节、转场术语章节、可能还有流媒体语法和物理字典的新增项 |
| **项目总览** | "Few-Shot 上游：标杆视频拉片"段落写了当前使用 v4、v4 的核心变化是什么、有哪些标签要求。Prompt 换代时总览必须反映新版本号、新能力、新要求 | v4→vN 的版本号、核心变化描述、拉片要求表格 |
| **目录导航** | "共享工具/提示词"章节按状态列出了 prompt 文件（当前入口 vs 历史版本）。新 prompt 要加入口，旧 prompt 要移位到历史版本 | 提示词表格 |
| **所有拉片脚本** | `openrouter_segment_analyze.py`、`gemini_native_analyze_30s.py` 等脚本内部硬编码了 prompt 文件路径。改路径或文件名时脚本必须同步 | 脚本内的 prompt 文件路径 |
| **已有拉片结果的备注** | C 已完成 v4 拉片，D 是 v2 产物。如果 v5 引入 incompatible 的变化（比如格式全换），需要在总览和导航里更新 D 的旧样本状态和是否仍可用的判断 | 项目总览的 Few-Shot 缺口表 |
| **补充提示词文件** | `共享工具/提示词/完整视频连续追问分段拉片补充提示词.txt` 等补充 Prompt 变化时，其依赖的版本假设、标签体系、输出格式约束也应同步审查 | 补充 Prompt 中的版本引用和标签定义 |
| **连续追问实验脚本** | `openrouter_fullvideo_followup_probe.py` 通过 `--prompt-file` 加载补充提示词。主 Prompt 或补充 Prompt 改版本号/文件名——脚本的命令行使用说明和工程计划里的示例命令要同步 | 脚本使用说明和工程计划中的 `--prompt-file` 引用路径 |

**不在影响范围内：**
- 分镜脚本本身（分镜用的是"分镜生成 Prompt"而非拉片 Prompt，两个是不同的 Prompt）
- 素材清单（素材清单是从分镜出来的，不直接碰拉片 Prompt）
- 渲染工程代码

---

### 链 2：工程标准（物理字典或素材协议）变更

**起点：** 修改 `工程标准_物理字典与素材协议.md`

| 受影响文件 | 原因 | 具体要改什么 |
|-----------|------|-------------|
| **所有分镜脚本** | 分镜脚本的生成规则是"只使用物理字典描述动画"。如果物理字典新增或删除了某个物理量（比如新增 `Skew`、删除 `ClipPath`），已有分镜和将来生成的分镜都受约束 | 已有分镜中的运动描述可能需要审查是否仍符合新版字典 |
| **所有素材清单** | 素材清单的分类体系（`GENERATE_BY_CODE` / `AI_BROWSER_CAPTURE` 等）直接对应工程标准的"资产分类与命名规范"。如果分类增加或改名，清单结构必须跟上 | 素材清单 JSON 的分类字段和 Markdown 概览 |
| **渲染工程** | 工程标准里的图库优先级（Pexels > Unsplash > Pixabay）影响步骤 3 的素材选择策略，从而间接影响渲染工程里引用的素材 | 非直接影响，但如果素材来源变了，渲染工程里的 public/assets symlink 可能需要更新 |
| **v4 Prompt** | 工程标准里的"拉片素材标签"（5 类）和"场景转场术语"是 Prompt 中强制要求的镜像。如果工程标准改了标签定义，Prompt 文件本身可能也要改——这是双向的 | Prompt 中的标签要求和转场类型列表 |
| **项目总览** | 总览的"Few-Shot 上游"段落引用了 v4 Prompt 的素材打标和原片/后期解耦要求——这些要求是从工程标准的物理字典约束中衍生出来的 | 可能需要同步更新总览中的相关描述 |

---

### 链 3：旁白稿变化

**起点：** 修改 `00_文稿/旁白稿.md`

| 受影响文件 | 原因 |
|-----------|------|
| **分镜脚本** | 旁白稿是分镜生成的唯一文字输入。旁白稿改了任意一段——分镜的 Timing、对应的画面内容、素材清单都要重新生成 |
| **素材清单** | 间接——分镜变了，素材自然变 |
| **渲染工程** | 间接——分镜和素材变了，渲染代码自然变 |
| **项目总览** | 如果旁白稿是新的版本号，总览可能需要更新"当前旁白稿"的引用 |

---

### 链 4：分镜脚本变化

**起点：** 修改或重新生成 `03_分镜脚本/完整分镜头脚本.md`

| 受影响文件 | 原因 |
|-----------|------|
| **素材清单** | 分镜脚本是素材清单的**唯一上游**。分镜的每一个图层引用了一个素材，清单从这些引用中提取。分镜新增/删除/重编号任何镜头——清单的 `ASSET_xx_xx` 编号体系就可能断裂 |
| **渲染工程** | 渲染代码基于分镜的逐秒描述实现动画。分镜的 timing、图层运动参数、素材引用一变，渲染代码要跟上 |
| **项目总览** | 总览的"当前进度"和"已知风险"可能涉及分镜质量 |
| **目录导航** | 导航的"视频项目"章节列出了当前分镜入口 |

---

### 链 5：素材清单变化

**起点：** 修改或重新生成 `04_素材清单/` 下的 JSON 和 MD 文件

素材编号（`ASSET_xx_xx`）是本链条的关键锚点。编号一变，以下五个渲染工程内的具体文件必须逐个对齐：

| 受影响文件 | 原因 |
|-----------|------|
| **`05_渲染工程/public/assets/ASSET_xx_xx.png`** | 目录导航第 150 行明确规定素材走 symlink。清单里的素材文件新增/重命名——symlink 目标必须指向新文件。不要在 `public/assets/` 里直接存源素材 |
| **`05_渲染工程/asset-manifest.json`** | 记录"ASSET 编号 → 磁盘文件名"的映射表。清单新增或重编号素材——manifest 必须追加或更新对应条目 |
| **`05_渲染工程/src/lib/assets.ts`** | TypeScript 类型表，定义了所有合法 asset ID 的字面量联合类型。清单新增素材编号——类型表必须同步扩展，否则渲染代码里引用新编号会编译报错 |
| **`05_渲染工程/scripts/check-assets.mjs`** | 检查脚本：扫描 manifest 和 public/assets/，确认所有引用文件存在、无一断裂。素材变更后应重跑此脚本 |
| **目录导航** | 导航列出了当前素材清单入口 |

---

### 链 6：目录结构调整

**起点：** 新建、删除、重命名项目下的目录或关键文件

| 受影响文件 | 原因 |
|-----------|------|
| **目录导航** | 导航就是所有文件路径的索引。目录变了导航不更新——导航就废了 |
| **项目总览** | 总览引用了其他核心文档的相对路径。文件名改为新的时间戳后——旧链接断 |
| **引用维护规则（目录导航第 157 行）** | 导航里已经写了一条 `rg` 命令用于检查重命名后的引用断裂。每次重命名都要跑那条命令 |

---

### 链 7：共享工具/脚本变化

**起点：** 修改 `共享工具/拆解脚本/` 下的 Python 脚本

| 受影响文件 | 原因 |
|-----------|------|
| **目录导航** | 导航"共享工具/拆解脚本"章节列出了每个脚本的用途 |

**起点：** 修改 `共享工具/库对比测试/conclusion.md`

| 受影响文件 | 原因 |
|-----------|------|
| **渲染工程** | conclusion.md 决定了"哪个物理效果用哪个库实现"（GSAP 主选、PixiJS 放大镜、anime.js ClipPath）。如果结论变了——渲染工程里的库选择策略可能也要变 |

---

### 链 8：Few-Shot 样本（标杆拉片）变化

**起点：** 新增或重新拆解一个标杆视频拉片

| 受影响文件 | 原因 |
|-----------|------|
| **项目总览** | 总览的"Few-Shot 上游"和"当前状态"段落记录了拉片样本分布。新拉片完成——更新 C/D/E/A/B 的状态，总览的"下一步优先事项"也可能调整优先级 |
| **目录导航** | 导航的"标杆视频库"章节描述了可用样本 |
| **分镜脚本** | 间接——Few-Shot 样本的质量和数量影响分镜生成的质量 |

---

### 链 9：原片与预处理（视频原片、source_meta、压缩参数）

**起点：** 标杆视频的原片文件变化（更换版本、重新下载、时长变化）、`source_meta.md` 更新、或压缩参数调整

| 受影响文件 | 原因 |
|-----------|------|
| **压缩版视频** | 原片换了或压缩参数改了（ffmpeg 的 scale、codec、码率）——压缩版视频必须重新生成。项目总览第 112-130 行定义了当前推荐的压缩参数和 base64 大小限制，任何偏离都可能导致 Qwen Code 等工具拒绝读取 |
| **`source_meta.md`** | 原片变化时——文件路径、时长、分辨率、格式等元数据字段必须同步更新 |
| **拉片分段策略** | 原片时长变化后——分段脚本的 segment 数量、每段起止时间、重叠窗口都可能需要调整 |
| **拉片结果** | 如果原片是同一个视频的高清版本（内容不变），已有拉片结果的时间锚点应验证是否仍然对齐；如果原片内容也变了——拉片结果需要重新生成 |
| **项目总览** | 总览的"当前状态"段落引用了具体的视频文件和拉片状态。原片或参数变了——这些引用值必须同步 |
| **目录导航** | 导航的"标杆视频库"章节和实验记录引用了具体视频目录和文件名 |

**不在影响范围内：**
- 工程标准（原片格式不影响物理字典和素材标签定义）
- 分镜脚本（原片内容的变化通过拉片结果间接影响，不直接触发）---

### 链 10：Proposal 工作流自身的变化 [活跃]

**状态：活跃。** Proposal 工作流由 Superpowers 驱动（brainstorming → writing-plans → executing-plans），产出写入 `_proposals/P00N_*.md`（AGENTS.md 已定义流程）。旧 Codex skill 实施计划（`2026-06-22_15-30_轻量Proposal工作流Skill实施计划.md`）已归档至 `_归档_/`，不代表 Proposal 废弃。

**起点：** 修改 `_proposals/` 目录结构或 AGENTS.md 中的 Proposal 流程规则

| 受影响文件 | 原因 |
|-----------|------|
| **目录导航** | 导航管理 `_proposals/` 目录入口 |
| **依赖关系图（本文件）** | 本文件描述 proposal 与依赖链的配合方式 |

---

## 三、AI Agent 使用本文件的方式

> **权威行为规则见 [AGENTS.md](./AGENTS.md) 的"Blast Radius 行为"章节。** 本节仅说明如何使用上面的依赖链数据。

使用步骤：
1. 收到修改意图时，对照上面的 11 条链判断起点属于哪条链（可能触发多条）
2. 从第四节速查表获取最少需改文件数，用作检查基线
3. 停住，把影响文件列表汇报给用户，等确认
4. 后续（逐文件执行 + git diff + rg 验证）按 AGENTS.md 的 Blast Radius 行为执行

---

## 四、常见场景的 blast radius 速查表

| 你说要改什么 | 自动触发链 | 最少需要改的文件数 |
|-------------|-----------|-------------------|
| 拉片 Prompt 升级到 v5 | 链 1 + 链 2（双向）| 5-7 个文件 |
| 物理字典新增/删除一个运动参数 | 链 2 | 3-5 个文件 |
| 素材标签分类改名 | 链 2 + 链 1（双向）| 4-6 个文件 |
| 编辑旁白稿 | 链 3 | 1 个文件直接 + 视情况触发链 4/5 |
| 重新生成分镜脚本 | 链 4 + 链 5 + 链 6 | 4-7 个文件 |
| 素材清单重建 | 链 5 | 2-3 个文件 |
| 新增一个标杆视频拉片 | 链 8 | 3-5 个文件 |
| 目录/文件重命名 | 链 6 | 3-4 个文件 |
| 库对比测试结论更新 | 链 7 | 2-3 个文件 |
| 原片换版本/压缩参数调整 | 链 9 | 4-7 个文件 |

---

## 五、与 Superpowers Proposal 的配合

> **当前状态：** Proposal 工作流由 Superpowers 驱动（brainstorming → writing-plans → executing-plans），产出写入 `_proposals/P00N_*.md`。本依赖关系图在 proposal 执行过程中作为 blast radius 分析工具。旧 Codex skill 实施计划已归档至 `_归档_/2026-06-22_15-30_轻量Proposal工作流Skill实施计划.md`。

两者的配合方式：
1. brainstorming 阶段澄清需求后，产出写入 `_proposals/P00N_*.md`
2. AI 读本文件，定位到对应依赖链，算出 blast radius
3. writing-plans 阶段在 proposal 文档的 Impact 段落写出完整影响文件列表（引用本文件的链编号）
4. executing-plans 阶段按 Change Plan 逐一执行
5. 执行完跑 `git diff --name-only` + `rg` 交叉引用验证

